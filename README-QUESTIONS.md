# README questions — needs Cole's input

1. **Problem provenance (PM line).** The template wants one sentence on how
   you knew the problem was real (own DJ workflow? user requests? a forum
   thread?). Nothing in the repo answers this. There's a
   `<!-- TODO: verify -->` in the draft where it belongs.

2. **License.** The old README claimed "UNLICENSED. See `LICENSE` file" — but
   no LICENSE file exists in the repo. The new draft omits the section. Add a
   license file, or leave it?

3. **Landing page claim.** The live landing page says "Trusted by 2,000+ DJs &
   producers" while the README now says 3,155 registered users. STATE.md
   already flags the landing line as unbacked — worth aligning the two so the
   site and README don't disagree.

4. **Traction source.** The numbers are date-stamped but not attributed. If
   you want to attribute them ("from the app's operation log" /
   "admin analytics"), confirm which source they came from.
