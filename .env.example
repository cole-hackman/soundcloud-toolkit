# SoundCloud Toolkit - Environment Variables
# Copy to .env and fill in your values

# --- Server (required for API to start) ---
SOUNDCLOUD_CLIENT_ID=your_soundcloud_client_id
SOUNDCLOUD_CLIENT_SECRET=your_soundcloud_client_secret
SOUNDCLOUD_REDIRECT_URI=http://localhost:3000/api/auth/callback
ENCRYPTION_KEY=32characterslongexactly32characters!
SESSION_SECRET=super_long_random_string_at_least_32_chars
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# --- App URLs (comma-separated for multiple origins) ---
APP_URL=http://localhost:3000
APP_URLS=http://localhost:3000,http://localhost:3001

# --- Optional ---
PORT=3001
NODE_ENV=development

# --- Frontend (Next.js) - only needed when API is on a different host ---
# NEXT_PUBLIC_API_BASE=http://localhost:3001
